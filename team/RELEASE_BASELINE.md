# RELEASE_BASELINE — open ops ledger (ops-release, P0)

Revision probed: `git rev-parse HEAD` = **31cb49623f001543241cbb92c21acd74ca1b10bb**
(branch `release/owned-staging-20260917`; tree NOT clean — 10 modified / 6 untracked).
Railway project `loop-gpt-owned-staging-20260917` / env `production` (id
`2faec73c-12aa-47c9-9a6c-94a9276eb6d5`); `railway status` → backend `● Online`,
deployment `28d0fa22-7bb2-4c59-adb9-8e120bab9b5d`.
All probes below were run 2026-09-26 from the project root; raw result pasted.
Secrets verified **by presence only** (`railway variables --service <svc> --kv | cut -d= -f1`).

Format: `item | source line | current state (raw probe) | command/step to close | blocked-on`

1. **Uptime probe on `/healthz` | docs/PROGRESS.md:101-106, 140-141 (1.3 "uptime monitor … needs an operator account") | OPEN (monitor), probe GREEN — `curl -s -o /dev/null -w '%{http_code}' https://loop-gpt.cyou/healthz` → `200` | probe: that curl; monitor: create an UptimeRobot/BetterStack free-tier HTTPS monitor, 60s, URL `https://loop-gpt.cyou/healthz`, expect 2xx | alerting needs an operator account (the probe itself needs nothing)**
2. **Observability confirmed by a deliberate test error | docs/PROGRESS.md:101-106 + 127-145 (1.3); AUDIT_REPORT.md:731-736 (P11) | DONE TODAY, both DSNs ingest — `SENTRY_DSN` (backend) + `NEXT_PUBLIC_SENTRY_DSN` (web) present as env NAMES; deliberate `store/` POST → `backend store endpoint -> HTTP 200`, `web store endpoint -> HTTP 200` | re-run: POST a JSON `{"level":"error",...}` to `https://<host>/api/<project>/store/?sentry_key=<key>&sentry_version=7` with the DSN parsed from `railway variables` (never echo the DSN); confirm the event in the Sentry issue stream | nothing (dashboard eyeball optional)**
3. **DB restore rehearsal | docs/PROGRESS.md:120-126 (Phase 1 close-out: "FULLY CLOSED … 33/33 tables") vs docs/RUNBOOK.md:143-144 ("Status: … scratch-restore rehearsal pending DATABASE_URL" — STALE) | CLOSED per PROGRESS, footer stale; `DATABASE_URL` IS present in the prod backend env (name confirmed); local `pg_dump`/`pg_restore` are NOT on PATH (`command -v pg_dump` → not found) | re-rehearsal: temporary public TCP proxy on the `postgres` service → `docker run -d --name loop-restore-scratch -e POSTGRES_PASSWORD=scratch -p 127.0.0.1:54329:5432 postgres:16-bookworm` → `pg_dump "$DATABASE_URL" --format=custom --file=loop-gpt-backup.dump` → `pg_restore -h 127.0.0.1 -p 54329 -U postgres -d postgres --no-owner --no-privileges --create loop-gpt-backup.dump` → `\dt` must show 33 tables; delete the proxy. Also fix `docs/RUNBOOK.md:144` footer | postgres client tools not installed locally (Docker path works); owner of RUNBOOK.md must apply the footer fix**
4. **Stripe — go-live or freeze | docs/STRIPE_LIVE_CHECKLIST.md:3-4 (decision), :20-48 (7-step go-live); AUDIT_REPORT.md:717-722 (P9) | FROZEN, verified live — `curl -s https://loop-gpt.cyou/api/billing/config` → `{"enabled":false,"checkoutEnabled":false,"fulfillmentEnabled":false,"fulfillmentImplemented":true,"publishableKey":null,"plans":{"pro":false,"gold":false},…}` HTTP 200; backend env NAMES contain `STRIPE_CHECKOUT_ENABLED`, `STRIPE_FULFILLMENT_ENABLED` and **no** `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_PRO`/`STRIPE_PUBLISHABLE_KEY` | freeze (already in effect): no code step — state is explicit in the checklist + honest UI; go-live: follow STRIPE_LIVE_CHECKLIST steps 1-7 (create Pro price → set the 7 env vars on backend → add the webhook endpoint `https://loop-gpt.cyou/api/billing/webhook` → redeploy → test-mode purchase → live 1-cent charge → restore landing copy) | **operator: live/test Stripe keys + the webhook signing secret (prices, too). Nothing to build.**
5. **Marketplace OAuth live smoke (Figma first) | docs/PROGRESS.md:87-90 (1.4; "live OAuth round-trips still open") + 24-28 (Q3/P8); AUDIT_REPORT.md:710-715 (P8), 1126 (Q10 Q3); docs/CONNECTOR_SETUP.md | OPEN — no live round-trip; stub CI shipped (44 provider tests, PROGRESS:82-90) | create a Figma OAuth app (docs/CONNECTOR_SETUP.md), paste clientId/clientSecret in Settings → Connectors → Marketplace, complete the PKCE round-trip, then call the generic `api_request` tool against `api.figma.com`; expect connector row written + a 200 tool result | **operator: a real Figma OAuth app (redirect `https://api.loop-gpt.cyou/api/oauth-connector/callback`)** 
6. **Deploy read-back procedure | team/P0_KICKOFF.md:61 (P4), docs/PROGRESS.md:146-147 (bundle-hygiene read-back) | PROCEDURE NOT RE-RUN; surface EXISTS — `curl -s https://loop-gpt.cyou/chat/ | grep -oE '_next/static/chunks/[A-Za-z0-9_./-]+\.js' | sort -u` returned the served fingerprint (e.g. `_next/static/chunks/2373-9f81c0cb321c7fba.js`, `2631-7754d38b30959869.js`, `263-49a9b04960f3e208.js`) | after any deploy: `git rev-parse HEAD` → `cd frontend && npm run build` → diff the served chunk-name set (command at left) against `out/_next/static/chunks/`; equal sets ⇒ served revision == built revision; then `curl -s -o /dev/null -w '%{http_code}' https://loop-gpt.cyou/healthz` = 200. A deploy is not done until the served set matches the built set | a dirty working tree — the read-back must be run against a clean build of the deployed commit, not the local WIP (commit/`git stash` first)**

## Closable with no new input

- **#2 observability deliberate test error** — closed today (both DSNs HTTP 200). Re-runnable at will.
- **#1 uptime probe** — the raw probe is green (`/healthz` → 200); only the alerting account is external.
- **#3 DB restore** — already closed per PROGRESS; only the stale RUNBOOK footer needs a one-line edit.

## Env presence (names only, verified, not printed)

- backend (59 names incl.): `DATABASE_URL`, `RESEND_API_KEY`, `MAIL_FROM`, `SENTRY_DSN`, `STRIPE_CHECKOUT_ENABLED`, `STRIPE_FULFILLMENT_ENABLED`, `OAUTH_CALLBACK_BASE`, `PUBLIC_API_URL`, `JWT_SECRET`, `HF_*`, `GOOGLE_*`, `GITHUB_*` — **absent: all `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_*`/`STRIPE_PUBLISHABLE_KEY`, and all `SMTP_*` (Resend-only transport; fallback unarmed)**.
- web: `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, `API_UPSTREAM`, `OWNED_WEB_ORIGIN` — present.
- Carried forward, still open (PROGRESS:654-660): `AGENT_DATA_DIR` is unset in the prod backend env ⇒ the tool audit log is container-local and resets on each deploy. Set `AGENT_DATA_DIR` under `/private-store` if deploy-surviving audit history is wanted.
