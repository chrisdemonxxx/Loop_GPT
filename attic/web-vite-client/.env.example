# Public build-time origin ONLY. No credentials. Empty = same-origin /api.
VITE_API_ORIGIN=
# Development proxy only; the backend already defaults to port 3001.
WEB_DEV_API_ORIGIN=http://127.0.0.1:3001
