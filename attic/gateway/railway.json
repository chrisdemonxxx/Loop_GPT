{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "sh -c 'RNS=$(awk \"/^nameserver/{print \\$2; exit}\" /etc/resolv.conf); [ -n \"$RNS\" ] || RNS=10.202.10.2; case \"$RNS\" in *:*) RNS=\"[$RNS]\";; esac; echo \"gateway resolver: $RNS\"; sed -i \"s/__DNS__/$RNS/\" /etc/nginx/conf.d/default.conf; exec nginx -g \"daemon off;\"'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
